<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="res_partner_form_view_customer_type" model="ir.ui.view">
        <field name="name">res.partner.form.customer.type</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='category_id']" position="after">
                <field name="customer_type"  invisible="not has_discount_rules"/>
                <field name="has_discount_rules" invisible="1"/>
            </xpath>
        </field>
    </record>

</odoo>
